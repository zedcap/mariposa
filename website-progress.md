# Mariposa website — build progress

Live tracker. To pick this project up in any session, read this file plus `website-scope.md`. Project state lives in files (here), not in conversation memory.

## Status

Build underway. Home page rebuilt against Stephane's "feels lazy / early-2000s" feedback (2026-05-21) — first cut was structure-first and restrained; the rebuild adds scroll choreography, full-bleed scale and a shared GSAP motion engine, modelled on the marigold.global theme. Home nav uses the marigold.global pattern (menu rests below the hero, sticks to the top on scroll). The rebuild is deployed and live at `https://zedcap.github.io/mariposa/` (2026-05-22) — see the Deploy phase for the SSH push transport now in use.

## Working files (all in this folder)

- `website-scope.md` — scope, structure, decisions. Source of truth.
- `website-copy.md` — Axel's web copy.
- The Astro project — `src/`, `package.json`, etc. Preview: `npm run dev` → `http://localhost:4321`.
- `website-progress.md` — this file.

## Decisions locked

- Astro 6 + TypeScript, `output: static`. No Tailwind — design-system CSS tokens wired as a global stylesheet.
- **GSAP sanctioned** for scroll choreography (Stephane, 2026-05-21). `gsap` + `ScrollTrigger`, ~47KB gzipped, shared across sections via one `motion` chunk. The standard tool; marigold.global uses the same. Loads deferred; page is fully readable without it.
- One-page-scroll home + 2 product pages + About page + 21+ age gate. No contact page.
- Product section: Option B (paired editorial slider).
- Web typography: Spectral + Hanken Grotesk (fallback to Cormorant + Inter only if that fails).
- Contacts: `hello@mariposacigars.com` + Instagram `@mariposacigars` — both contingent on availability.
- No forms, no CMS, no e-commerce, English only.
- Footer: no legal-entity line. Product pages: no trade-enquiry line.
- Framing: credibility artefact — hero front-loads the brand impression hard and fast.
- Nav: marigold.global pattern — menu sits below the hero imagery, sticks to the top on scroll.

## Build phases

- [done] Dispatch 1 — Astro scaffold + home page first cut (age gate, hero slider, product section, lifestyle section, sticky contact, footer).
- [done] Home nav revision — marigold.global menu-below-hero-then-sticks pattern. Header is `position: sticky; top: 0` in document flow after the hero (BaseLayout named `hero` slot). Rests below the hero on load, sticks at the top on scroll; gains 24px blur + faint shadow when stuck. Hero-less pages render the header stuck from the top (dispatch 2 inherits this).
- [done] Home revisions — Stephane review 1 (2026-05-21). Eight items:
  1. Hero height — `64svh` contained band (was `100svh`), matched to marigold.global's shorter hero. *(Superseded by the rebuild — see below; the contained band read as a header strip, not a hero.)*
  2. Hero text overlay removed — no wordmark/position/sub-line on the slider images. *(Held in the rebuild.)*
  3. Scroll cue removed. *(Reinstated in the rebuild as a quiet drifting gold tick — a "keep scrolling" affordance the choreographed page now earns.)*
  4. Product-section eyebrow + gold divider removed; the slider stands alone. *(Held in the rebuild.)*
  5. Nav uppercase (VOLAGE, CAPRICE, ABOUT). SKU corrected to **Caprice** (singular) — name, slug, route `/caprice`, asset, content file all renamed.
  6. Brand position copy ("Cuban Soul. 24 Carat Swiss Gold." + sub-line) moved off the hero into a new `PositionStatement.astro` text block between hero and product slider; treatment mirrors the brand pager masthead.
  7. Lifestyle section uses `mariposa_header_sq.png` (= `src/assets/header-collage.png`), replacing the placeholder.
  8. Colour correction — site-wide. `--nav-bg` was cream `rgba(239,234,226,0.78)`; replaced with white-based `rgba(255,255,255,0.82)`. Header comments de-creamed. Conventions overlay updated: design system's cream nav-bar / `--cream-edge` explicitly overridden, white only. No other cream values found in the site (the `tokens.css` copy had already renamed `--ivory`→`--white`, `--cream-edge`→`--hairline`).
- [done] **Home rebuild — Stephane review 2, "feels lazy / early-2000s" (2026-05-21).** First cut was built structure-first and restrained; correct for the print pagers, wrong for a "cool, modern lifestyle brand" (the governing principle, scope §1). Rebuilt for scroll choreography, full-bleed scale and analogue motion, mining the marigold.global Shopify theme for technique.
  - **GSAP added.** `src/lib/motion.ts` — one shared motion engine. Registers `ScrollTrigger`; exposes `reveal` (one-shot rise-and-fade), `revealLines` (masked headline unveil — the Marigold text-animation move), `slideIn` (masked horizontal slide), `parallax` and `scaleScrub` (scrub-tied depth/settle — the Marigold title-break / hero-image move), `scrubTimeline`. Honours `prefers-reduced-motion` throughout — every animated element degrades to its instant resting state. `refreshOnLoad` recomputes triggers after webfonts settle.
  - **Hero.** Restored to full-bleed `100svh` (the contained 64svh band was the main "header strip" tell). *(Height superseded 2026-05-22 — see "Hero height contained" below; the 100svh hero hid the nav menu, contradicting the marigold.global menu-below-hero pattern. Reverted to a contained band.)* Each slide carries a slow Ken Burns scale + drift; cross-fade kept; the gold ticks now carry a fill bar that runs over the dwell so the slider's pace is legible; a quiet drifting gold scroll cue. No text on the imagery (held).
  - **Position statement.** Rebuilt from a small centred block into a tall section the line commands — display scale, the sentence unveiled line-by-line through clipping masks, a gold hairline that draws itself in.
  - **Product slider.** Option B kept (resolved). Each frame is now a composed editorial spread: a giant ghosted display initial behind the cigar (depth), the cigar on a parallax drift, copy that cascades in when the frame activates, the pink hairline drawn across, a kinetic slide between frames, pointer-swipe + arrow-key support. No section heading (held — review 1 item 4).
  - **Lifestyle section.** Rebuilt from a tidy 1:1 column grid into an immersive editorial spread: a tall image bed with parallax + scale-settle, a pearl story card that lifts off the image and breaks the grid line (asymmetry), copy revealing paragraph-by-paragraph, the closing line unveiled at display scale through masks.
  - **Header.** Contracts on stick (84px → 66px) — the Marigold nav move. `scroll-padding-top` updated to match (66/56px).
  - **Footer.** Brand line reveals on scroll-in so the page close is part of the choreography.
  - Build clean; home page ships ~47KB gzipped JS (GSAP is the bulk), deferred. HTML ~19KB.
- [done] **Hero colour treatment removed — Stephane's call (2026-05-22).** The duotone `filter: grayscale(1) sepia(0.26) contrast(1.05) brightness(0.8)` on the hero images read as a gold tint. Removed: hero imagery now renders in original colour, no filter. The gold cross-fade wash is dropped — slide transitions are a plain cross-fade. Scrims: the top scrim is removed (the nav rests below the hero, so it had no remaining job); a minimal foot scrim is kept (dialled from `rgba(28,25,21,0.5)`/30% to `0.34`/24%) so the gold slide-indicator ticks and scroll cue stay legible against a light image. A deliberate override of the design system's "full-bleed photography = B&W/duotone" rule — recorded in `mariposa-design-conventions.md` (new "Hero / full-bleed photography (website)" section). Ken Burns scale, scroll choreography, four images and slider motion all unchanged. Deployed — see Deploy phase.
- [done] **Hero height contained — Stephane's call (2026-05-22).** The rebuild had restored the hero to full-bleed `100svh`. That contradicts the site's nav pattern: the marigold.global pattern places the menu *below* the hero (sticks to the top on scroll), and that placement only reads if the menu is visible below the hero on first paint. A 100svh hero pushed the menu under the fold — the "menu below hero" placement was invisible until the user scrolled. Hero capped to a contained band so the nav bar shows underneath on load: `height: 64svh; min-height: 440px; max-height: 720px` desktop; `height: 60svh; min-height: 360px; max-height: 560px` on phones (≤720px) so the bar stays visible on short viewports too. Not a regression to the old "header strip" — the band stays rich: four images, Ken Burns scale, scroll choreography, slider motion and original-colour imagery all unchanged; only the height is capped. The 84px nav bar (66px mobile) shows clearly below the hero on standard desktop and phone viewports. Deployed — see Deploy phase.
- [done] **Hero images swapped — 4 placeholders → 3 real Mariposa images (2026-05-22).** Stephane supplied three finished Mariposa collage hero images (`mp_header_1/2/3.jpg`, source `  creative assets/headers/`) — the brand device: same faceless woman with a cigar on three worlds (palm poolside / jungle hillside lodge / Bali pavilion over water). The slider now runs THREE frames; the four placeholder frames — three of them Marigold's own photographs — are fully removed from the build. New assets `src/assets/hero-mariposa-1/2/3.jpg`; `home.json` `hero.slides` cut 4→3 (data-driven, the single switch); `HeroSlider.astro` imports updated, Marigold-placeholder banner comment removed, responsive `widths` trimmed to `[720,1024,1250]` for the 1250×720 source. Band height, Ken Burns, choreography, cross-fade, original-colour treatment all unchanged. Build clean. Deployed — `624a60f`, Actions run `26279545411` success, live URL verified serving 3 frames. Images are ~1.74:1; the band is wider on desktop so `object-fit: cover` crops top/bottom — per-image `object-position` nudge available if any frame clips the figure/cigar. Old `hero-image0/1/2/3.jpg` remain unreferenced in `src/assets/` — prune at a later tidy-up.
- [done] **Hero polish — slider dots + mobile aspect-ratio (2026-05-22).** Two changes after Stephane reviewed the live 3-image hero.
  - **Slide indicator → dots.** The three gold ticks/lines became three 10px round dots, centred at the hero foot (`left:50%` + `translateX`). Active dot solid gold (`--gold-leaf`), inactive translucent white. The ticks' fill-bar pace cue was reworked as a thin conic-gradient progress ring around the active dot, driven by a `--dwell` custom property over the 7s dwell; reduced-motion / JS-off fall back to a static full ring. Click-to-jump, `role="tab"`, focus states all preserved.
  - **Mobile hero → native aspect-ratio, no crop.** Stephane's call: instead of `object-fit: cover` cropping the wide images into a fixed-height mobile band, the mobile hero (≤720px) takes the images' native ratio (`aspect-ratio: 125/72`; all three are 1250×720) and scales down proportionally — full image visible, uncropped. Mobile uses `object-fit: contain`; fixed `svh` height and min/max removed on mobile. Desktop unchanged (contained band, `cover` crop). The earlier slide-3 `object-position: 25%` mobile crop-nudge is removed — redundant with no crop. Dots and scroll cue pulled in on mobile (dots 24→12px, cue 48→24px / line 56→36px) for the shorter band. Resulting mobile band height = viewport width × 72/125 (≈216px at 375px wide, ≈184px at 320px) — not floored, proportional per Stephane's brief.
- [done] **Hero 3→6 images + lifestyle brandmark swap (2026-05-23).** Stephane supplied a new wider hero set and a new brandmark.
  - **Hero — six images, BW→colour transition.** Replaced the three `hero-mariposa-1/2/3.jpg` with six new frames from `  creative assets/headers/`, used in this exact order: `header_1_bw → header_3_bw → header_2_bw → header_1 → header_3 → header_2`. Three B&W treatments first (scenes 1, 3, 2), then the same three scenes in colour (1, 3, 2). The slider becomes a transition device, monochrome → colour. Order intentional, preserved. New assets in `src/assets/` (`hero-mariposa-1/2/3-bw.jpg`, `hero-mariposa-1/2/3-color.jpg`); `home.json` `hero.slides` 3→6 (data-driven — dots and progress-ring auto-pick six from `slides.length`); fresh per-frame captions / alt text.
  - **Aspect ratio change.** New images are 3200×1800 (16:9), prior set was 1250×720 (~1.74:1). Mobile `aspect-ratio` constant updated `125/72 → 16/9` to keep the no-crop mobile behaviour matching the new image dimensions. Responsive `widths` array updated `[720,1024,1250] → [720,1280,1920,2560]` to use the new 3200px native (2560 cap for high-DPI without shipping full 3200). Desktop band behaviour unchanged (contained 64svh + cover crop).
  - **Lifestyle section brandmark.** Stephane retired `mariposa_header_sq.png` and replaced it with `  creative assets/mariposa_brandmark_sq.jpg`. The lifestyle section's `header-collage.png` reference was swapped to a new `brandmark-sq.jpg` (913×913) asset; `LifestyleSection.astro` updated. The existing duotone treatment still applies; square aspect cover-fits the wide lifestyle bed unchanged.
  - Loop length flag: 6 × 7s = 42s loop. Yulia's lean is keep 7s — slow pace serves the BW→colour reveal. Stephane's call if he wants it tighter.
  - Old `hero-mariposa-1/2/3.jpg` and `header-collage.png` remain on disk unreferenced — sweep at tidy-up.
  - Typo fix in source folder: `heade_1_bw.jpg` → `header_1_bw.jpg` (missing `r`).
  - Build clean.
- [done] **Position statement polish + pink token update (2026-05-23).**
  - **Position statement** (`PositionStatement.astro`). Spacing tightened — top padding set to nav-bar height (84px desktop / 66px mobile), prior `min-height: 86svh` vertical-centring removed. Bottom padding made symmetric (84/66) so the block reads with one nav-height of breathing room on both sides. Headline "Cuban Soul. 24 Carat Swiss Gold." split into two halves with directional entrance — "Cuban Soul." slides in from left, "24 Carat Swiss Gold." from right (uses `slideIn` from `motion.ts`, masked, reduced-motion respected). Gold-leaf SVG divider added between headline and sub-line: `clamp(64px, 8vw, 96px)` width, centred, static, no draw-in. Asset `src/assets/divider-gold-leaf.svg` copied from design system. Mariposa-wordmark and prior gold-rule already removed in the previous polish pass. Note: the design-conventions print rule advises against `divider-gold-leaf.svg` on documents — Stephane explicitly chose it for web; web-scoped use is fine.
  - **Pink token global swap.** `--pink` changed `#DF6899 → #d06f98` (deeper / less saturated mauve-pink). Option 1 of three — single-token swap, `--pink-deep` (`#C24A82`) and `--pink-mist` (`#F2A5A6`) untouched. Palette ordering still holds (new `--pink` darker / less saturated than old but still lighter than `--pink-deep`). Propagated across website (`src/styles/tokens.css`; components using `var(--pink)` pick up automatically) AND the design system master (`Claude Design - Design System/colors_and_type.css`), the three print pagers (brand/Volage/Caprice), and design-system previews (`colors-pink.html`, `colors-semantic.html`). Out-of-website-repo files updated on disk; not part of this website deploy. No remaining `#DF6899` literals anywhere.
- [done] **Cream lifestyle section + cream-adaptive nav (2026-05-23).** Lifestyle section background → `#f2f0e9` (the cream Stephane briefly tried on the position statement and reverted; now repositioned here, where it belongs). The boundary from the white product slider above is a gradient bleed — top-of-section `.story__bleed` wedge, `clamp(120px, 18vh, 220px)` tall, pearl `#FAFAF7` → 60% pearl → transparent — so the dark image bed emerges from the pearl rather than reading as a banded edge. The bed's own bottom fade was retargeted white → `#f2f0e9` so the image dissolves into the cream cleanly. Sticky nav adopts `rgba(242,240,233,0.82)` when overlapping the cream section: `IntersectionObserver` on `[data-cream-bg]`, `rootMargin: -72px 0 0 0` (covers both 66px desktop and 56px mobile stuck-bar heights), threshold 0, toggles `.is-over-cream` on `.site-header`, CSS transition 200ms `var(--ease-paper)`. Reduced-motion snaps instantly. **Convention conflict to record:** `mariposa-design-conventions.md` (lines 42, 44) explicitly rejects cream backgrounds + cream nav-bars; the overlay needs updating to log the lifestyle-section + over-cream-nav exception so doctrine and build stop disagreeing. Files: `LifestyleSection.astro`, `Header.astro`.
- [done] **Cream zone corrective — moved from lifestyle-only to product+lifestyle (2026-05-23).** Stephane sent a screenshot of the position-statement / product-slider boundary saying "this section with the product still is the same as before, and there's a straight line. It doesn't gradually appear." The previous round had applied the cream + gradient + nav-observer to the lifestyle section (one section below), missing his actual intent. Corrected: cream `#f2f0e9` now starts at the product slider section (under Volage) and continues through lifestyle to the footer. ProductSlider: section background swapped from `var(--bg-elevated)` (pearl) to cream; `.range__bleed` gradient wedge added at top of the section (`clamp(120px, 18vh, 220px)`, white → transparent) so the boundary from the white position statement above warms in; viewport + controls given explicit `z-index: 1` so they sit above the wedge. LifestyleSection: now sits directly below another cream section, so the `.story__bleed` wedge added last round became redundant and was removed (image-bed bottom-wash to cream stays). Header: IntersectionObserver rewired to **two-target with a Set-based aggregator** — `data-cream-bg` on BOTH product and lifestyle sections; the header's `.is-over-cream` class flips off only when the set is empty, so the nav stays cream continuously across the seam between the two adjacent cream sections (no flicker), reverting only when the footer takes over. Pink hairline, `--bg-elevated` token, type, motion, imagery untouched. Files: `ProductSlider.astro`, `LifestyleSection.astro`, `Header.astro`.
- [done] **Footer trim + rhythm (2026-05-23).** Removed the email and Instagram links (contact lives in the sticky element) and the 21+ / responsible-enjoyment line ("You must be 21 or older to enjoy Mariposa. Please smoke responsibly." — the age gate already handles entry). Removed the now-orphaned `.site-footer__top`, `.site-footer__contact`, `.site-footer__legal` blocks. Footer rhythm tightened to one unit (`--footer-rhythm: var(--space-7)` = **48px**), per Stephane's brief: gap above brand line = gap below = gap above divider = gap below divider = X. Footer top padding `--space-9` (96px) → 48px; divider `margin-block` 48px each side; bottom padding 48px (already, now via the token). Wordmark + copyright sit in the existing bottom row (flex space-between desktop, stacked mobile); 48px stack gap on mobile already matches the rhythm. `global.json` still carries `responsibleLine` (passthrough schema, no other consumer). File: `Footer.astro`.
- [done] **Nav colour-tracks-scroll, opaque (2026-05-23).** Replaced the snap-based IntersectionObserver / `.is-over-cream` class flip with continuous scroll-driven background-colour interpolation. Mechanism: `requestAnimationFrame`-throttled scroll listener (not GSAP — lighter for a single per-frame colour sample), reads `.range__bleed` (the white→cream gradient bleed wedge at the top of the product slider) live via `getBoundingClientRect`, calculates `k = (wedgeBottom - navBottom) / wedgeHeight` clamped [0, 1], interpolates between `--nav-white` and `--nav-cream`. Outside the wedge: opaque white above, opaque cream inside the cream zone, snap back to white on cream→footer (clean horizontal cut, no return gradient). **Opaque** — `rgba(...,0.82)` alpha + `backdrop-filter: blur(24px)` removed per Stephane's "not transparent, but the same colour as what's underneath" rule. Existing `border-bottom` + stuck-state shadow carry the separation. Orphan `--nav-bg` / `--nav-blur` tokens retired in `tokens.css` with comment. Reduced-motion snaps to endpoint. Files: `Header.astro`, `tokens.css`.
- [done] **Footer brand line on two lines (2026-05-23).** "Don't wait for special occasions." on line 1, "Create them." on line 2. Deterministic break via a `<span class="site-footer__brandline-tail">` with `display: block` wrapping the tail; `global.json` brandLine string untouched. Lifestyle closing-line already splits per sentence as masked blocks, reads identically — no change there. File: `Footer.astro`.
- [done] **Footer brand-line wrap fix + position-statement bottom tightened (2026-05-23).** Footer: brand line was three-lining on narrow viewports ("Don't wait for special / occasions. / Create them."). Both sentences now wrap in `display: block; white-space: nowrap` spans, and the size is fluid `clamp(18px, 5.6vw, --fs-h4)` so the lead sentence stays on one line at narrow widths (floor 18px, ceiling 28px at ~500px viewport, desktop pinned). `max-width: 22ch` removed (would fight nowrap). Position statement: bottom padding reduced ~33% (84→56 desktop, 66→44 mobile) to tighten the gap into the Volage frame. Deliberately breaks last round's top/bottom symmetry. Top padding unchanged. Files: `Footer.astro`, `PositionStatement.astro`.
- [done] **Volage product frame iteration (2026-05-23).** Three changes in one round.
  - **Image swap:** Volage product image switched from the object PNG `cigar-volage.png` → editorial scene `cigar-volage-terrasse.jpg` (Volage cigar in glass ashtray, wooden table, palm-fringed sunset; 840×1120, 3:4 portrait). `ProductSlider.astro` gained a per-slug `imageMode` map (`volage: 'scene'`, `caprice: 'object'`, default `'object'`) flowing into BEM modifiers. Scene-mode CSS: `padding: 0` (edge-to-edge fill), `object-fit: cover` with `object-position: 50% 60%`, drop-shadow off, grain knocked from 0.05 → 0.025. Frame aspect is `4/5` vs photo `3/4` — `cover` crops modestly off top/bottom (sky/palm-trunk height); cigar/gold-leaf/ashtray fully preserved. Ghosted "V" letter sibling sits to the left of the frame, not over the photo — depth motif preserved. Old `cigar-volage.png` orphaned on disk.
  - **Display name size:** Volage + Caprice `.range__name` dropped `clamp(3rem, 6.5vw, 5.25rem)` → `clamp(1.9rem, 4.8vw, 3.4rem)`, mirroring the position-statement headline (`PositionStatement.astro` line 88). Shared class so both products track. Typeface, weight, tracking, motion untouched.
  - **Volage homeLede updated:** "A long, slow Canonazo built for the hour you give yourself, creamy at the first draw, full-bodied to the last." (dropped "The flagship." opener; "by the last" → "to the last").
  - **Composition flag from Yulia:** Volage now reads as editorial / lifestyle while Caprice still reads as catalogue (cigar-on-void PNG). They no longer rhyme. Three options: live with it (Volage leads as flagship), source a scene shot for Caprice and flip its imageMode, or pull both back to object-mode. Decision deferred.
  - Files: `ProductSlider.astro`, `volage.json`, new asset `src/assets/cigar-volage-terrasse.jpg`.
- [done] **Image refresh + asset cleanup + hero dot simplification (2026-05-23).** Stephane re-edited seven source images and asked for a clean-up. Six hero JPEGs (`header_1/2/3.jpg`, `header_1/2/3_bw.jpg`) + Volage `volage_terrasse.jpg` overwritten in `src/assets/`; dimensions unchanged (3200×1800 heroes, 840×1120 Volage), no component edits needed. Nine orphaned assets removed from `src/assets/`: `hero-image0–3.jpg` (Marigold placeholders), `hero-mariposa-1/2/3.jpg` (first MP hero set, superseded by bw/color set), `cigar-volage.png` (superseded), `header-collage.png` (superseded by `brandmark-sq.jpg`). Folder now carries only the 10 live files. Hero dot indicator simplified: stripped the conic-gradient progress ring + GSAP `--dwell` tween entirely — active dot solid gold (`var(--gold-leaf)`), inactive solid white (`var(--white)`), no translucency, no progress affordance. Click-to-jump, `role="tab"`, focus states, reduced-motion fallback preserved. Files: `HeroSlider.astro`, `src/assets/`.
- [done] **Brand position re-cast + Volage frame iteration #2 (2026-05-23).**
  - **Brand position line re-cast.** "Cuban Soul. 24 Carat Swiss Gold." → "Volcanic Soul. 24-Carat Gold." — ties the position to Java's volcanic terroir (Argopuro range), which Volage's new long lede also now references explicitly. Net string changes: "Cuban" → "Volcanic" (everywhere it referred to the position), "24 Carat Swiss Gold" → "24-Carat Gold" (drop "Swiss", hyphenate). Swept across **18 files / 23 replacements** spanning website (`home.json`, `global.json`, `BaseLayout.astro`, `pages/index.astro`, `PositionStatement.astro` comments, `website-copy.md`, `website-scope.md`), design system (README, SKILL, brand/copy.md, four `ui_kits/website/` files, two preview HTMLs), and the print brand pager. **Heritage / craft language preserved** ("Cuban triple cap", "Cuban tradition", "Cuban heritage", "Cuba's torcedoras", "Cuban genetics" in body copy / About / Origins) — that's varietal and craft fact, not the brand position. Residual flag: design-system `type-pairings.html` carries the pun "(Swiss Gold, Swiss grotesque.)" — typography rationale built on the old line, now stale; not auto-rewritten, Stephane to decide.
  - **Vitola label replaces edition badge, no box, both products.** "1ST EDITION" (boxed pill, Volage) → "Canonazo" (unboxed eyebrow). "Q4 2026" (boxed muted pill, Caprice) → "Gordito" (unboxed eyebrow). Same eyebrow treatment for both: `--fs-eyebrow`, `font-weight: 600`, `letter-spacing: var(--ls-eyebrow)`, uppercase, `--gold-deep`, no border / padding / radius. `range__tags` / `range__tag` / `range__tag--muted` CSS removed; `.range__vitola` rule added + GSAP entrance cascade updated. `edition` / `tag` JSON fields preserved (other surfaces may consume).
  - **Volage-only type-size step-down** (Caprice kept at current sizes so Stephane can compare side-by-side). Mechanism: `.range__frame--volage` modifier added; three overrides scoped to it. `.range__name` `clamp(1.9rem,4.8vw,3.4rem)` → `clamp(1.6rem,4.0vw,2.8rem)`. `.range__tagline` `clamp(1.25rem,2.4vw,1.6rem)` → `clamp(1.05rem,2.0vw,1.35rem)`. `.range__lede` `--fs-body` (17px) → `--fs-small` (14px); lede `max-width` 40ch → 46ch to breathe the longer paragraph.
  - **Volage new long lede:** "An elegant, slow-burning journey for the senses. It opens with a remarkably creamy profile, seamlessly transitioning into layers of earth, wood, and dark coffee. A soft sweetness lingers on the palate just as a sophisticated spice emerges. Cultivated in the shadow of the Argopuro range, the filler leverages Java's volcanic terroir, offering a unique Indonesian expression of classic Havana-seed tobacco." (~64 words vs previous ~26; card has room at desktop, grows in flow on mobile.) Curly apostrophe normalised to straight per project convention.
  - Files (website, deploy-relevant): `ProductSlider.astro`, `volage.json`, `home.json`, `global.json`, `BaseLayout.astro`, `pages/index.astro`, `PositionStatement.astro`, `website-copy.md`, `website-scope.md`. Files (out-of-repo, on-disk only): print brand pager + design-system README / SKILL / brand-copy / preview HTMLs / UI-kit JSX / index.html.
- [pending] Dispatch 2 — product pages (`/volage`, `/caprice`), About page (`/about`).
- [pending] Imagery — Stephane commissions from the shot list in `website-scope.md` §7. Placeholders until then.
- [done] Deploy — GitHub Pages staging **LIVE**: `https://zedcap.github.io/mariposa/`. Repo `zedcap/mariposa` (public); CI workflow `deploy.yml` builds and deploys on every push to `main`. First successful deploy 2026-05-21 (run 26219302974, both build + deploy jobs green). Pages source set to "GitHub Actions" by Stephane. Verified: home page serves HTTP 200 with the age gate + hero; CSS, images and inline client JS all resolve correctly under the `/mariposa/` base.
  - Push credential: fine-grained PAT in `/Users/mrclaude/Claude/.git-repos/mariposa-credentials.json` (chmod 600, outside pCloud, outside repo). Git host-scoped credential helper reads it; token not in `.git/config`. **PAT expires ~2026-08-19 (90 days from 2026-05-21) — renew before then.**
- [done] **Deploy rebuild — `3e26e9e` (home rebuild) + `84e106c` (hero images) LIVE** (2026-05-22, Sergey). Both commits on `origin/main`; Actions run `26257970367` (`deploy.yml`, SHA `84e106c`) completed **success**; `https://zedcap.github.io/mariposa/` verified serving the rebuilt site — HTTP 200, all four hero images resolve (`_astro/hero-image0..3.*.webp`, 84-225KB each), rebuilt component scripts present (`HeroSlider`, `ProductSlider`, `PositionStatement`, `LifestyleSection`), not the pre-rebuild first cut.
  - **Transport is now SSH.** HTTPS push stalled on the Bali-to-GitHub link in the pack-upload phase (ref discovery clean, upload POST hung); see history below. Remote URL is permanently switched to `git@github.com:zedcap/mariposa.git`. An ed25519 deploy key (registered on the repo with write access) is the working transport.
  - **Standing push command — all future pushes from Mr. Claude must use this** (no persistent git config, no `~/.ssh/config` changes):
    ```
    GIT_SSH_COMMAND="ssh -i /Users/mrclaude/Claude/.git-repos/mariposa-deploy-key -o IdentitiesOnly=yes" git push origin main
    ```
    Same `GIT_SSH_COMMAND` prefix applies to `git fetch` / `git pull`. Private key: `/Users/mrclaude/Claude/.git-repos/mariposa-deploy-key` (chmod 600, outside pCloud, outside repo). Do not push over HTTPS — it stalls.
  - Note: the two commits are authored under git's auto-identity `MrClaude <mrclaude@MrClaude-MBP.local>` (no repo identity set). Left as-is — rewriting history not worth it.
  - **History (why SSH).** HTTPS `git push` from this machine stalls in the pack-upload phase: `GIT_TRACE_CURL` showed ref discovery completing cleanly (TLS + auth + advertisement 200 OK in ~1.4s), then the upload `POST .../git-receive-pack` never returns and the connection hangs. Reads (`git ls-remote`) were instant. ~10-15MB of objects over a high-latency link with a buffered chunked POST. Did not fix it: plain push, HTTP/1.1, `http.postBuffer` to 1GB, credential helper, sandbox off, low-speed-limit tolerances — all stalled identically. SSH uses a single bidirectional stream and survives the latency.

## Open / watch

- Gemini critic MCP not available to Axel as a subagent — manual adversarial read used as fallback. Sergey to check subagent MCP mounting.
- Domain `mariposacigars.com` and IG `@mariposacigars` not yet secured — verify before launch.
- **Hosting migration planned for launch.** Build phase uses GitHub Pages (`zedcap/mariposa`, public repo, auto-deploy) for the frictionless staging URL. At launch, hosting moves off GitHub Pages to wherever makes most sense — Namecheap (one vendor with the domain) is the leading option; decide at launch. Migration is a clean one-time move: drop the Astro `base` config back to root, rebuild, upload the static folder, point the domain. No lock-in. Git store lives outside pCloud at `/Users/mrclaude/Claude/.git-repos/mariposa-website.git/`.
- Imagery is placeholder throughout until Stephane's commissioned photography lands. Exceptions: the hero slider now runs three real Mariposa collage images (see build phase), and the lifestyle section uses `mariposa_header_sq.png` (the brand composition) per Stephane's review. Product-page and remaining slots stay on placeholders.
- **Hero imagery — RESOLVED (2026-05-22).** The four placeholder frames (three of them Marigold's own photographs) are removed. The hero runs three real Mariposa collage images supplied by Stephane (`hero-mariposa-1/2/3.jpg`); the "another brand's imagery on the live site" risk is closed. Open sub-item: Stephane to eyeball the live crop — landscape images in a wider band crop top/bottom; a per-image `object-position` nudge is available if any frame clips. Orphaned `hero-image0-3.jpg` still on disk, unreferenced — prune at tidy-up.
- **SKU naming reconciled (2026-05-22).** The plural "Caprices" / `/caprices` is fully retired across the print and working surfaces, matching the singular ruling. `website-scope.md` and `website-copy.md` corrected; the Volage and Caprice pagers corrected; the print pager renamed `mariposa - Caprices (2026-05-20).html` → `mariposa - Caprice (2026-05-20).html`. Residual: the creative-asset PNG is still named `mariposa-caprices.png` — copy reads "Caprice", asset filename stays plural; left untouched to avoid breaking pager `src` references. Final commissioned photography can land on `mariposa-caprice.png` if the asset name should track the SKU.
